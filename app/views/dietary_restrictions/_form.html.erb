<%= form_for(@dietary_restriction) do |f| %>
  <% if @dietary_restriction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dietary_restriction.errors.count, "error") %> prohibited this dietary_restriction from being saved:</h2>

      <ul>
      <% @dietary_restriction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
